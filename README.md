Project structure

shorten - take long url and shorten it
analytics - analytics of url like click count
[code] - redirect the user after clicking the shorten url

prisma - prisma cli
base62 - the algorithm to shorten the url

service - CRUD of the urls

